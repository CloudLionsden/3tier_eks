apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tier3-app-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tier3-app-service
                port:
                  number: 80
